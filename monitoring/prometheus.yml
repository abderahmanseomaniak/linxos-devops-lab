global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: "linxos-app"
    metrics_path: "/api/metrics"
    scheme: https

    static_configs:
      - targets:
          - "linxos-devops-lab.vercel.app"